<Window x:Class="LSS_prototype.Login_Page.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:LSS_prototype.Login_Page"
        mc:Ignorable="d"
        Title="Admin Credential Change"
        Width="960" Height="900"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">

    <Window.Resources>
        <SolidColorBrush x:Key="Bg"           Color="#F3F5F7"/>
        <SolidColorBrush x:Key="Primary"      Color="#173753"/>
        <SolidColorBrush x:Key="PrimaryHover" Color="#1F4767"/>
        <SolidColorBrush x:Key="PrimaryDown"  Color="#102A3F"/>
        <SolidColorBrush x:Key="InputBg"      Color="#FFFFFF"/>
        <SolidColorBrush x:Key="InputBorder"  Color="#D6DEE8"/>
        <SolidColorBrush x:Key="TextDark"     Color="#1E293B"/>
        <SolidColorBrush x:Key="TextMuted"    Color="#64748B"/>

        <Style x:Key="InputBoxBorderStyle" TargetType="Border">
            <Setter Property="Background"          Value="{StaticResource InputBg}"/>
            <Setter Property="BorderBrush"         Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness"     Value="2"/>
            <Setter Property="CornerRadius"        Value="12"/>
            <Setter Property="Height"              Value="64"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontFamily"               Value="Arial"/>
            <Setter Property="FontSize"                 Value="24"/>
            <Setter Property="Background"               Value="Transparent"/>
            <Setter Property="BorderThickness"          Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding"                  Value="18,0"/>
            <Setter Property="Foreground"               Value="{StaticResource TextDark}"/>
        </Style>

        <Style x:Key="InputPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="FontFamily"               Value="Arial"/>
            <Setter Property="FontSize"                 Value="24"/>
            <Setter Property="Background"               Value="Transparent"/>
            <Setter Property="BorderThickness"          Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding"                  Value="18,0,64,0"/>
            <Setter Property="Foreground"               Value="{StaticResource TextDark}"/>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource Primary}"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="FontFamily"      Value="Arial"/>
            <Setter Property="FontSize"        Value="24"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height"          Value="64"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12" Padding="18,10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDown}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background"      Value="White"/>
            <Setter Property="Foreground"      Value="{StaticResource Primary}"/>
            <Setter Property="FontFamily"      Value="Arial"/>
            <Setter Property="FontSize"        Value="24"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Height"          Value="64"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource InputBorder}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12" Padding="18,10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="FontSize"   Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="Margin"     Value="2,0,0,10"/>
        </Style>

        <Style x:Key="CapsLockStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="FontSize"   Value="17"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#EF4444"/>
            <Setter Property="Margin"     Value="14,0,0,10"/>
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>

        <Style x:Key="RoleComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="FontSize"                  Value="18"/>
            <Setter Property="Height"                    Value="48"/>
            <Setter Property="VerticalContentAlignment"  Value="Center"/>
            <Setter Property="Padding"                   Value="15,0"/>
        </Style>

        <Style x:Key="RoleComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background"                          Value="Transparent"/>
            <Setter Property="BorderThickness"                     Value="0"/>
            <Setter Property="FontSize"                            Value="18"/>
            <Setter Property="Foreground"                          Value="#222222"/>
            <Setter Property="Padding"                             Value="15,0"/>
            <Setter Property="HorizontalContentAlignment"          Value="Left"/>
            <Setter Property="VerticalContentAlignment"            Value="Center"/>
            <Setter Property="Cursor"                              Value="Hand"/>
            <Setter Property="MaxDropDownHeight"                   Value="260"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility"   Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="45"/>
                            </Grid.ColumnDefinitions>
                            <ToggleButton Grid.ColumnSpan="2"
                                          Background="Transparent" BorderThickness="0"
                                          Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <ContentPresenter Grid.Column="0"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center" HorizontalAlignment="Left"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              IsHitTestVisible="False"/>
                            <Border Grid.Column="1" Background="#F2F4F7" CornerRadius="0,10,10,0" IsHitTestVisible="False">
                                <Path Data="M 0 0 L 6 6 L 12 0 Z" Fill="#1A3B5D"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <Popup x:Name="Popup" Placement="Bottom" VerticalOffset="5"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                <Border Background="White" BorderBrush="#DDE2E8" BorderThickness="1.5"
                                        CornerRadius="10" MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource Bg}" CornerRadius="16" SnapsToDevicePixels="True">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="720">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="26"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="18"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="18"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="18"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="22"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 타이틀 -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Center">
                <TextBlock Text="ADMIN CREDENTIAL CHANGE"
                           FontFamily="Arial" FontSize="48" FontWeight="Bold"
                           Foreground="{StaticResource Primary}" HorizontalAlignment="Center"/>
                <Border Height="4" Width="200" Background="{StaticResource Primary}"
                        CornerRadius="2" HorizontalAlignment="Center" Margin="0,18,0,0"/>
                <TextBlock Text="First-time login: Please update your ID and password."
                           FontFamily="Arial" FontSize="18" Foreground="#94A3B8"
                           HorizontalAlignment="Center" Margin="0,14,0,0"/>
            </StackPanel>

            <!-- LOGIN ID -->
            <StackPanel Grid.Row="2">
                <TextBlock Text="LOGIN ID" Style="{StaticResource LabelStyle}"/>
                <Border Style="{StaticResource InputBoxBorderStyle}">
                    <TextBox x:Name="LoginIdBox"
                             Text="{Binding LoginId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource InputTextBoxStyle}" Loaded="LoginIdBox_Loaded"/>
                </Border>
            </StackPanel>

            <!-- NEW PASSWORD 레이블 -->
            <Grid Grid.Row="4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="NEW PASSWORD"
                           Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
                <TextBlock x:Name="CapsLockWarningNew" Grid.Column="1"
                           Text="Caps Lock is ON. Password is case-sensitive."
                           Style="{StaticResource CapsLockStyle}" VerticalAlignment="Center"/>
            </Grid>

            <!-- NEW PASSWORD 입력 -->
            <Border Grid.Row="6" Style="{StaticResource InputBoxBorderStyle}">
                <Grid>
                    <PasswordBox x:Name="NewPasswordBox"
                                 Style="{StaticResource InputPasswordBoxStyle}"
                                 GotKeyboardFocus="NewPasswordBox_CheckCaps"
                                 PreviewKeyUp="NewPasswordBox_CheckCaps"
                                 LostKeyboardFocus="NewPasswordBox_HideCaps"/>
                    <TextBox x:Name="NewPasswordVisible"
                             FontFamily="Arial" FontSize="24"
                             Background="Transparent" BorderThickness="0"
                             Padding="18,0,64,0" Foreground="{StaticResource TextDark}"
                             VerticalContentAlignment="Center"
                             IsReadOnly="True" Visibility="Collapsed"/>
                    <Button Width="64" Height="64"
                            HorizontalAlignment="Right" VerticalAlignment="Center"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            PreviewMouseDown="BtnShowNewPw_MouseDown"
                            PreviewMouseUp="BtnShowNewPw_MouseUp">
                        <TextBlock Text="👁" FontSize="28"/>
                    </Button>
                </Grid>
            </Border>

            <!-- CONFIRM PASSWORD 레이블 -->
            <Grid Grid.Row="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="CONFIRM PASSWORD"
                           Style="{StaticResource LabelStyle}" VerticalAlignment="Center"/>
                <TextBlock x:Name="CapsLockWarningConfirm" Grid.Column="1"
                           Text="Caps Lock is ON. Password is case-sensitive."
                           Style="{StaticResource CapsLockStyle}" VerticalAlignment="Center"/>
            </Grid>

            <!-- CONFIRM PASSWORD 입력 -->
            <Border Grid.Row="10" Style="{StaticResource InputBoxBorderStyle}">
                <Grid>
                    <PasswordBox x:Name="ConfirmPasswordBox"
                                 Style="{StaticResource InputPasswordBoxStyle}"
                                 GotKeyboardFocus="ConfirmPasswordBox_CheckCaps"
                                 PreviewKeyUp="ConfirmPasswordBox_CheckCaps"
                                 LostKeyboardFocus="ConfirmPasswordBox_HideCaps"/>
                    <TextBox x:Name="ConfirmPasswordVisible"
                             FontFamily="Arial" FontSize="24"
                             Background="Transparent" BorderThickness="0"
                             Padding="18,0,64,0" Foreground="{StaticResource TextDark}"
                             VerticalContentAlignment="Center"
                             IsReadOnly="True" Visibility="Collapsed"/>
                    <Button Width="64" Height="64"
                            HorizontalAlignment="Right" VerticalAlignment="Center"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            PreviewMouseDown="BtnShowConfirmPw_MouseDown"
                            PreviewMouseUp="BtnShowConfirmPw_MouseUp">
                        <TextBlock Text="👁" FontSize="28"/>
                    </Button>
                </Grid>
            </Border>

            <!-- ROLE 레이블 -->
            <TextBlock Grid.Row="12" Text="ROLE" Style="{StaticResource LabelStyle}"/>

            <!-- ROLE 콤보박스 -->
            <Border Grid.Row="14" Style="{StaticResource InputBoxBorderStyle}">
                <ComboBox x:Name="RoleComboBox"
                          Style="{StaticResource RoleComboBoxStyle}"
                          ItemContainerStyle="{StaticResource RoleComboBoxItemStyle}"
                          SelectedValue="{Binding Role, UpdateSourceTrigger=PropertyChanged}"
                          SelectedValuePath="Content">
                    <ComboBoxItem Content="Physician"/>
                    <ComboBoxItem Content="Therapist"/>
                    <ComboBoxItem Content="Nurse"/>
                    <ComboBoxItem Content="Technician"/>
                    <ComboBoxItem Content="Researcher"/>
                    <ComboBoxItem Content="Other"/>
                </ComboBox>
            </Border>

            <!-- 버튼 -->
            <Grid Grid.Row="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="SAVE"
                        Style="{StaticResource PrimaryButtonStyle}" Click="Save_Click"/>
                <Button Grid.Column="2" Content="CANCEL"
                        Style="{StaticResource SecondaryButtonStyle}" Click="Cancel_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>

<UserControl x:Class="LSS_prototype.Views.PatientAddDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="330" d:DesignWidth="300">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="20">
            <TextBlock Text="Patient Name" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding PatientName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Height="26"/>

            <TextBlock Text="Patient Code" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding PatientCode, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Height="26"/>

            <TextBlock Text="Date of Birth" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding BirthDate, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Height="26"/>

            <TextBlock Text="Sex" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Sex, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" Height="26"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
            <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Margin="0,0,30,0" Height="29"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80"/>
        </StackPanel>
    </Grid>
</UserControl>
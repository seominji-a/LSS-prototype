<Window x:Class="LSS_prototype.Patient"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PATIENT" Height="1080" Width="1920" 
        WindowState="Maximized" WindowStyle="None" ResizeMode="NoResize" Topmost="True">

    <Grid Background="#FFE5E5E5">
        <TabControl x:Name="MainTabControl" Style="{StaticResource TabCustomStyle}">

            <TabItem x:Name="TabScheduled" 
                     Header="Scaduled Patient" 
                     Style="{StaticResource TabItemCustomStyle}">

                <Grid Margin="30,40,30,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="250"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock x:Name="TxtScheduledTitle" Grid.Row="0" Text="PATIENT LIST" FontSize="60" Margin="30,57,0,0"/>

                        <StackPanel Grid.Row="1" Margin="30,10,0,30">
                            <TextBlock Text="Scheduled Patient" FontSize="40" Foreground="#FF483E3E" Margin="6,0,0,10"/>
                            <Rectangle Height="14" Fill="#FF483E3E" RadiusX="15.54" RadiusY="15.54" Margin="0,0,20,0"/>
                        </StackPanel>

                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                            <UniformGrid x:Name="ScheduledGrid" Columns="3" VerticalAlignment="Top" Margin="0,0,20,0">
                                <Border Style="{StaticResource PatientCardStyle}"/>
                                <Border Style="{StaticResource PatientCardStyle}"/>
                                <Border Style="{StaticResource PatientCardStyle}"/>
                                <Border Style="{StaticResource PatientCardStyle}"/>
                                <Border Style="{StaticResource PatientCardStyle}"/>
                                <Border Style="{StaticResource PatientCardStyle}"/>
                                <Border Style="{StaticResource PatientCardStyle}"/>
                                <Border Style="{StaticResource PatientCardStyle}"/>
                                <Border Style="{StaticResource PatientCardStyle}"/>
                            </UniformGrid>
                        </ScrollViewer>
                    </Grid>

                    <StackPanel Grid.Column="1" VerticalAlignment="Top" Margin="0,296,0,0">
                        <Button x:Name="BtnSync" Content="Sync" 
                                Style="{StaticResource DefaultRoundedButtonStyle}" 
                                Command="{Binding SyncClickCommand}"/>
                        <Button x:Name="BtnHomeScheduled" Content="Home" 
                                Style="{StaticResource DefaultRoundedButtonStyle}" 
                                Click="HomeButton_Click"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem x:Name="TabRegistered" 
                     Header="Registered Patient" 
                     Style="{StaticResource TabItemCustomStyle}">

                <Grid Margin="30,40,30,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="250"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock x:Name="TxtRegisteredTitle" Grid.Row="0" Text="PATIENT LIST" FontSize="60" Margin="30,57,0,0"/>

                        <StackPanel Grid.Row="1" Margin="30,10,0,30">
                            <TextBlock Text="Registered Patient" FontSize="40" Foreground="#FF483E3E" Margin="6,0,0,10"/>
                            <Rectangle Height="14" Fill="#FF483E3E" RadiusX="15.54" RadiusY="15.54" Margin="0,0,20,0"/>
                        </StackPanel>

                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding RegisteredPatients}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="3" VerticalAlignment="Top" Margin="0,0,20,0"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource PatientCardStyle}" Margin="10">
                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontSize="35" FontWeight="Bold" HorizontalAlignment="Center"/>
                                                <TextBlock Text="{Binding PatientCode}" FontSize="35" Foreground="Gray" HorizontalAlignment="Center"/>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
                                                    <TextBlock Text="{Binding BRITH_DATE, StringFormat=yyyy-MM-dd}"/>
                                                    <TextBlock Text=" / "/>
                                                    <TextBlock Text="{Binding Sex}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>

                    <StackPanel Grid.Column="1" VerticalAlignment="Top" Margin="0,296,0,0">
                        <Button x:Name="BtnAdd" Content="Add Patient" 
                                Style="{StaticResource DefaultRoundedButtonStyle}" 
                                Command="{Binding PatientAddCommand}"/>
                        <Button x:Name="BtnEdit" Content="Edit Patient" 
                                Style="{StaticResource DefaultRoundedButtonStyle}" 
                                Command="{Binding PatientEditCommand}"/>
                        <Button x:Name="BtnDelete" Content="Delete Patient" 
                                Style="{StaticResource DefaultRoundedButtonStyle}"/>
                        <Button x:Name="BtnHomeRegistered" Content="Home" 
                                Style="{StaticResource DefaultRoundedButtonStyle}" 
                                Click="HomeButton_Click"/>
                    </StackPanel>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>
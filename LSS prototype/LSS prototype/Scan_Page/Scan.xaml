<UserControl x:Class="LSS_prototype.Scan_Page.Scan"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:LSS_prototype.Common_Module"
             mc:Ignorable="d" d:DesignWidth="1920" d:DesignHeight="1080">

    <UserControl.Resources>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <SolidColorBrush x:Key="Primary"    Color="#173753"/>
        <SolidColorBrush x:Key="Panel"      Color="#1C2B3A"/>
        <SolidColorBrush x:Key="Card"       Color="#243447"/>
        <SolidColorBrush x:Key="CardBorder" Color="#2E4460"/>
        <SolidColorBrush x:Key="ValueBg"    Color="#1A2D3E"/>
        <SolidColorBrush x:Key="BtnDark"    Color="#2A3F55"/>
        <SolidColorBrush x:Key="Muted"      Color="#64748B"/>
        <SolidColorBrush x:Key="Inactive"   Color="#94A3B8"/>
        <SolidColorBrush x:Key="Accent"     Color="#3B82F6"/>
        <SolidColorBrush x:Key="PmBg"       Color="#1E3A5F"/>

        <!-- ══ 공통 Path 데이터 ══ -->
        <!-- + : 십자 모양 (팔 두께 8px, 전체 30x30) -->
        <Geometry x:Key="PlusGeometry">M 17,0 H 23 V 17 H 40 V 23 H 23 V 40 H 17 V 23 H 0 V 17 H 17 Z</Geometry>
        <!-- − : 가로 막대 (두께 8px, 너비 20px → 중앙 정렬용 20x30 캔버스) -->
        <Geometry x:Key="MinusGeometry">M 0,17 H 48 V 23 H 0 Z</Geometry>

        <!-- ══ Button Styles ══ -->
        <Style x:Key="BaseBtn" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PmBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="{StaticResource BtnDark}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize"   Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="DarkBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="#1E3A5F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize"   Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="IconBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="{StaticResource BtnDark}"/>
            <Setter Property="Width"      Value="62"/>
            <Setter Property="Height"     Value="62"/>
        </Style>

        <Style x:Key="ExitBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="#111827"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize"   Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Width"      Value="Auto"/>
            <Setter Property="Height"     Value="62"/>
        </Style>

        <Style x:Key="NavBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="{StaticResource Primary}"/>
            <Setter Property="FontSize"   Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height"     Value="72"/>
            <Setter Property="Width"      Value="200"/>
        </Style>

        <Style x:Key="NavBtnActive" TargetType="Button" BasedOn="{StaticResource NavBtn}">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border Background="Black" Opacity="0.2" CornerRadius="12" Margin="0,2,0,-2"/>
                            <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                <StackPanel VerticalAlignment="Center">
                                    <ContentPresenter HorizontalAlignment="Center"/>
                                    <Border Width="40" Height="3" Background="White" Margin="0,4,0,0" CornerRadius="2"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TogLA" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Background"      Value="{StaticResource Accent}"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10,0,0,10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TogLI" TargetType="Button" BasedOn="{StaticResource TogLA}">
            <Setter Property="Background" Value="{StaticResource BtnDark}"/>
            <Setter Property="Foreground" Value="{StaticResource Inactive}"/>
        </Style>

        <Style x:Key="TogRA" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Background"      Value="{StaticResource Accent}"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0,10,10,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="TogRI" TargetType="Button" BasedOn="{StaticResource TogRA}">
            <Setter Property="Background" Value="{StaticResource BtnDark}"/>
            <Setter Property="Foreground" Value="{StaticResource Inactive}"/>
        </Style>

        <Style x:Key="LedTogL" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Background"      Value="{StaticResource Accent}"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LedTogLI" TargetType="Button" BasedOn="{StaticResource LedTogL}">
            <Setter Property="Background" Value="{StaticResource BtnDark}"/>
            <Setter Property="Foreground" Value="{StaticResource Inactive}"/>
        </Style>

        <Style x:Key="LedTogR" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Background"      Value="{StaticResource Accent}"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LedTogRI" TargetType="Button" BasedOn="{StaticResource LedTogR}">
            <Setter Property="Background" Value="{StaticResource BtnDark}"/>
            <Setter Property="Foreground" Value="{StaticResource Inactive}"/>
        </Style>

        <!-- ══ PmButton Style: + / − Path 꽉 차게 (상단 CornerRadius 없음) ══ -->
        <Style x:Key="PmIncBtn" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource PmBg}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- − 버튼: 하단 CornerRadius -->
        <Style x:Key="PmDecBtn" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource PmBg}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0,0,11,11">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TransparentBtn" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ══ Animations ══ -->
        <Storyboard x:Key="NavIn">
            <DoubleAnimation Storyboard.TargetName="NavBar"  Storyboard.TargetProperty="Height"
                             From="0" To="90" Duration="0:0:0.01"/>
            <DoubleAnimation Storyboard.TargetName="NavTT"   Storyboard.TargetProperty="X"
                             From="1200" To="0" Duration="0:0:0.28">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        <Storyboard x:Key="NavOut">
            <DoubleAnimation Storyboard.TargetName="NavTT"   Storyboard.TargetProperty="X"
                             From="0" To="1200" Duration="0:0:0.22">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="NavBar"  Storyboard.TargetProperty="Height"
                             From="90" To="0" Duration="0:0:0.01" BeginTime="0:0:0.21"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="390"/>
        </Grid.ColumnDefinitions>

        <!-- ══════════════════════════════════════════
             카메라 영역
        ══════════════════════════════════════════ -->
        <Grid Grid.Column="0">
            <Border Background="Black">
                <Image x:Name="CameraPreviewImage"
               Stretch="Uniform"
               Source="{Binding PreviewSource}"/>
            </Border>


            <!-- 위아래 2분할 감마 컨트롤 오버레이 -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Background="#01000000" BorderThickness="0" Cursor="Hand" Style="{StaticResource TransparentBtn}"
                Command="{Binding GainIncCommand}"/>
                <Button Grid.Row="1" Background="#01000000" BorderThickness="0" Cursor="Hand" Style="{StaticResource TransparentBtn}"
                Command="{Binding GainDecCommand}"/>
            </Grid>

            <!-- 좌상단: 환자 정보 -->
            <StackPanel Margin="16,12" VerticalAlignment="Top" HorizontalAlignment="Left">
                <TextBlock Foreground="White" FontSize="18" FontWeight="Bold">
                    <Run Text="{Binding SelectedPatient.PatientName}"/>
                    <Run Text="("/>
                    <Run Text="{Binding SelectedPatient.PatientCode}"/>
                    <Run Text=")"/>
                </TextBlock>

                <TextBlock Foreground="White" FontSize="16">
                    <Run Text="{Binding SelectedPatient.BirthDate, StringFormat=yyyy-MM-dd}"/>
                    <Run Text=" / "/>
                    <Run Text="{Binding SelectedPatient.Sex}"/>
                </TextBlock>
            </StackPanel>

            <!-- 우상단: Gain / Sharpness -->
            <StackPanel Margin="0,12,16,0" VerticalAlignment="Top" HorizontalAlignment="Right">
                <TextBlock Text="Gain"      Foreground="#AAA" FontSize="16" HorizontalAlignment="Right"/>
                <TextBlock Text="{Binding GainText, FallbackValue='GAIN VALUE'}" Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Right"/>
                <TextBlock Text="Sharpness" Foreground="#AAA" FontSize="16" HorizontalAlignment="Right" Margin="0,6,0,0"/>
                <TextBlock Text="{Binding Sharpness, FallbackValue='SHARP VALUE'}"
                   Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Right"/>
            </StackPanel>

            <!-- 하단 Nav Bar -->
            <Grid x:Name="NavBar" VerticalAlignment="Bottom" Height="0" ClipToBounds="True">
                <Border Background="#E61C2B3A"/>
                <Grid>
                    <Grid.RenderTransform>
                        <TranslateTransform x:Name="NavTT" X="1200"/>
                    </Grid.RenderTransform>
                    <StackPanel Orientation="Horizontal" Margin="16,0" VerticalAlignment="Center">
                        <Button Content="Patient"      Style="{StaticResource NavBtn}"       Command="{Binding NavigatePatientCommand}" Margin="0,0,10,0"/>
                        <Button Content="Scan"         Style="{StaticResource NavBtnActive}" Margin="0,0,10,0"/>
                        <Button Content="Image Review" Style="{StaticResource NavBtn}"       Margin="0,0,10,0"/>
                        <Button Content="Video Review" Style="{StaticResource NavBtn}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
        <!-- ══════════════════════════════════════════
             우측 패널
        ══════════════════════════════════════════ -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="90"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource Panel}">
                <Grid Margin="10,8,10,8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="52"/>
                        <!-- R0: FILTER + COLOR MAP -->
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="200"/>
                        <!-- R2: LED 카드 2열 -->
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="200"/>
                        <!-- R4: ZOOM / FOCUS 2열 -->
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="52"/>
                        <!-- R6: AUTO FOCUS -->
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="*"/>
                        <!-- R8: 썸네일 -->
                    </Grid.RowDefinitions>

                    <!-- ════════════════════════════════
                         R0: FILTER ON/OFF + COLOR MAP
                    ════════════════════════════════ -->
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                Background="{StaticResource Card}"
                                BorderBrush="{StaticResource CardBorder}"
                                BorderThickness="1" CornerRadius="10" ClipToBounds="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="1"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0" x:Name="FilterOnBtn"
                                        Content="FILTER ON" Style="{StaticResource TogLA}"
                                        Height="52" FontSize="20"
                                        Background="{Binding FilterOnBackground}"
                                        Foreground="White"
                                        Command="{Binding FilterOnCommand}"/>
                                <Border Grid.Column="1" Background="{StaticResource CardBorder}"/>
                                <Button Grid.Column="2" x:Name="FilterOffBtn"
                                        Content="FILTER OFF" Style="{StaticResource TogRA}"
                                        Height="52" FontSize="20"
                                        Background="{Binding FilterOffBackground}"
                                        Foreground="White"
                                        Command="{Binding FilterOffCommand}"/>
                            </Grid>
                        </Border>

                        <Button Grid.Column="2"
                                Content="COLOR&#10;MAP"
                                Style="{StaticResource DarkBtn}"
                                Background="{StaticResource Primary}"
                                FontSize="18" Height="52"
                                Command="{Binding ColorMapCommand}"/>
                    </Grid>

                    <!-- ════════════════════════════════
                         R2: 700 LED + 800 LED
                    ════════════════════════════════ -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- ── 700 LED 카드 ── -->
                        <Border Grid.Column="0"
                                Background="{StaticResource Card}"
                                BorderBrush="{StaticResource CardBorder}"
                                BorderThickness="1" CornerRadius="12" ClipToBounds="True">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="36"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- 헤더 -->
                                <Grid Grid.Row="0" Margin="12,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="700 LED"
                                               Foreground="White" FontSize="13" FontWeight="Bold"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="(54)"
                                               Foreground="White" FontSize="15" FontWeight="Bold"
                                               VerticalAlignment="Center"/>
                                </Grid>
                                <Border Grid.Row="1" Background="{StaticResource CardBorder}"/>

                                <!-- ON/OFF | +/- -->
                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="1"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- ON / OFF -->
                                    <Grid Grid.Column="0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="1"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Button Grid.Row="0" x:Name="Led700OnBtn"
                                                Content="ON" Style="{StaticResource LedTogL}"
                                                FontSize="28" FontWeight="Bold"
                                                Background="{StaticResource Accent}" Foreground="White"
                                                Command="{Binding Led700OnCommand}"/>
                                        <Border Grid.Row="1" Background="{StaticResource CardBorder}"/>
                                        <Button Grid.Row="2" x:Name="Led700OffBtn"
                                                Content="OFF" Style="{StaticResource LedTogL}"
                                                FontSize="28" FontWeight="Bold"
                                                Background="{StaticResource BtnDark}" Foreground="{StaticResource Inactive}"
                                                Command="{Binding Led700OffCommand}"/>
                                    </Grid>
                                    <Border Grid.Column="1" Background="{StaticResource CardBorder}"/>

                                    <!-- + / − -->
                                    <Grid Grid.Column="2">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="1"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <!-- + Inc -->
                                        <Button Grid.Row="0" Style="{StaticResource PmIncBtn}"
                                                Command="{Binding Led700IncCommand}">
                                            <Viewbox Stretch="Uniform" Margin="15">
                                                <Path Fill="White" Data="{StaticResource PlusGeometry}"/>
                                            </Viewbox>
                                        </Button>
                                        <Border Grid.Row="1" Background="{StaticResource CardBorder}"/>

                                        <!-- − Dec -->
                                        <Button Grid.Row="2" Style="{StaticResource PmDecBtn}"
                                                Command="{Binding Led700DecCommand}">
                                            <Viewbox Stretch="Uniform" Height="20" Margin="20,0,20,12" VerticalAlignment="Center">
                                                <Path Fill="White" Data="{StaticResource MinusGeometry}"/>
                                            </Viewbox>
                                        </Button>
                                    </Grid>
                                </Grid>
                            </Grid>
                        </Border>

                        <!-- ── 800 LED 카드 ── -->
                        <Border Grid.Column="2"
                                Background="{StaticResource Card}"
                                BorderBrush="{StaticResource CardBorder}"
                                BorderThickness="1" CornerRadius="12" ClipToBounds="True">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="36"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- 헤더 -->
                                <Grid Grid.Row="0" Margin="12,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="800 LED"
                                               Foreground="White" FontSize="13" FontWeight="Bold"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="(32)"
                                               Foreground="White" FontSize="15" FontWeight="Bold"
                                               VerticalAlignment="Center"/>
                                </Grid>
                                <Border Grid.Row="1" Background="{StaticResource CardBorder}"/>

                                <!-- ON/OFF | +/- -->
                                <Grid Grid.Row="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="1"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- ON / OFF -->
                                    <Grid Grid.Column="0">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="1"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Button Grid.Row="0" x:Name="Led800OnBtn"
                                                Content="ON" Style="{StaticResource LedTogL}"
                                                FontSize="28" FontWeight="Bold"
                                                Background="{StaticResource Accent}" Foreground="White"
                                                Command="{Binding Led800OnCommand}"/>
                                        <Border Grid.Row="1" Background="{StaticResource CardBorder}"/>
                                        <Button Grid.Row="2" x:Name="Led800OffBtn"
                                                Content="OFF" Style="{StaticResource LedTogL}"
                                                FontSize="28" FontWeight="Bold"
                                                Background="{StaticResource BtnDark}" Foreground="{StaticResource Inactive}"
                                                Command="{Binding Led800OffCommand}"/>
                                    </Grid>
                                    <Border Grid.Column="1" Background="{StaticResource CardBorder}"/>

                                    <!-- + / − -->
                                    <Grid Grid.Column="2">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="1"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>

                                        <!-- + Inc -->
                                        <Button Grid.Row="0" Style="{StaticResource PmIncBtn}"
                                                Command="{Binding Led800IncCommand}">
                                            <Viewbox Stretch="Uniform" Margin="15">
                                                <Path Fill="White" Data="{StaticResource PlusGeometry}"/>
                                            </Viewbox>
                                        </Button>
                                        <Border Grid.Row="1" Background="{StaticResource CardBorder}"/>

                                        <!-- − Dec -->
                                        <Button Grid.Row="2" Style="{StaticResource PmDecBtn}"
                                                Command="{Binding Led800DecCommand}">
                                            <Viewbox Stretch="Uniform" Height="20" Margin="20,0,20,12" VerticalAlignment="Center">
                                                <Path Fill="White" Data="{StaticResource MinusGeometry}"/>
                                            </Viewbox>
                                        </Button>
                                    </Grid>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- ════════════════════════════════
                         R4: ZOOM + FOCUS
                    ════════════════════════════════ -->
                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- ── ZOOM 카드 ── -->
                        <Border Grid.Column="0"
                                Background="{StaticResource Card}"
                                BorderBrush="{StaticResource CardBorder}"
                                BorderThickness="1" CornerRadius="12" ClipToBounds="True">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="36"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- 헤더 -->
                                <Grid Grid.Row="0" Margin="12,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="ZOOM"
                                               Foreground="White" FontSize="13" FontWeight="Bold"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding ZoomText, FallbackValue='CURRENT'}"
                                               Foreground="White" FontSize="15" FontWeight="Bold"
                                               VerticalAlignment="Center"/>
                                </Grid>
                                <Border Grid.Row="1" Background="{StaticResource CardBorder}"/>

                                <!-- + Inc -->
                                <Button Grid.Row="2" Style="{StaticResource PmIncBtn}"
                                        Command="{Binding ZoomIncCommand}">
                                    <Viewbox Stretch="Uniform" Margin="10">
                                        <Path Fill="White" Data="{StaticResource PlusGeometry}"/>
                                    </Viewbox>
                                </Button>
                                <Border Grid.Row="3" Background="{StaticResource CardBorder}"/>

                                <!-- − Dec -->
                                <Button Grid.Row="4" Style="{StaticResource PmDecBtn}"
                                        Command="{Binding ZoomDecCommand}">
                                    <Viewbox Stretch="Uniform" Height="20" Margin="20,0,20,12" VerticalAlignment="Center">
                                        <Path Fill="White" Data="{StaticResource MinusGeometry}"/>
                                    </Viewbox>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- ── FOCUS 카드 ── -->
                        <Border Grid.Column="2"
                                Background="{StaticResource Card}"
                                BorderBrush="{StaticResource CardBorder}"
                                BorderThickness="1" CornerRadius="12" ClipToBounds="True">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="36"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="1"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- 헤더 -->
                                <Grid Grid.Row="0" Margin="12,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="FOCUS"
                                               Foreground="White" FontSize="13" FontWeight="Bold"
                                               VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding FocusText, FallbackValue='CURRENT'}"
                                               Foreground="White" FontSize="15" FontWeight="Bold"
                                               VerticalAlignment="Center"/>
                                </Grid>
                                <Border Grid.Row="1" Background="{StaticResource CardBorder}"/>

                                <!-- + Inc -->
                                <Button Grid.Row="2" Style="{StaticResource PmIncBtn}"
                                        Command="{Binding FocusIncCommand}">
                                    <Viewbox Stretch="Uniform" Margin="10">
                                        <Path Fill="White" Data="{StaticResource PlusGeometry}"/>
                                    </Viewbox>
                                </Button>
                                <Border Grid.Row="3" Background="{StaticResource CardBorder}"/>

                                <!-- − Dec -->
                                <Button Grid.Row="4" Style="{StaticResource PmDecBtn}"
                                        Command="{Binding FocusDecCommand}">
                                    <Viewbox Stretch="Uniform" Height="20" Margin="20,0,20,12" VerticalAlignment="Center">
                                        <Path Fill="White" Data="{StaticResource MinusGeometry}"/>
                                    </Viewbox>
                                </Button>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- ════════════════════════════════
                         R6: AUTO FOCUS
                    ════════════════════════════════ -->
                    <Button Grid.Row="6"
                            Content="Auto Focus"
                            Style="{StaticResource DarkBtn}"
                            Background="{StaticResource Primary}"
                            FontSize="15"
                            Command="{Binding AutoFocusCommand}"/>

                    <!-- ════════════════════════════════
                         R8: 썸네일 영역
                    ════════════════════════════════ -->
                    <Grid Grid.Row="8" x:Name="ThumbArea">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="6"/>
                                <ColumnDefinition Width="110"/>
                            </Grid.ColumnDefinitions>

                            <!-- 썸네일 2개 -->
                            <Grid Grid.Column="0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="6"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- 이미지 썸네일 -->
                                <Button Grid.Row="0"
            Command="{Binding ImageCommentCommand}"
            BorderThickness="0" Padding="0" Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="#0A1929" CornerRadius="10" ClipToBounds="True">
                                                <Image Source="{Binding ImageThumbnail}" Stretch="Uniform"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                                <!-- 영상 썸네일 -->
                                <Button Grid.Row="2"
            Command="{Binding VideoCommentCommand}"
            BorderThickness="0" Padding="0" Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="#0A1929" CornerRadius="10" ClipToBounds="True">
                                                <Image Source="{Binding VideoThumbnail}" Stretch="Uniform"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                            </Grid>

                            <!-- 우측 버튼 3개 -->
                            <Grid Grid.Column="2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="2*"/>
                                    <RowDefinition Height="6"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="6"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Button Grid.Row="0" Style="{StaticResource DarkBtn}" Command="{Binding ImageScanCommand}">
                                    <TextBlock Text="Image&#10;Scan" Foreground="White" TextAlignment="Center" FontSize="25"/>
                                </Button>
                                <Button Grid.Row="2" Command="{Binding VideoRecordCommand}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource DarkBtn}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsVideoRecording}" Value="True">
                                                    <Setter Property="Background" Value="#DC2626"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <StackPanel HorizontalAlignment="Center">
                                        <!-- 기본 상태 (녹화 중 아닐 때) -->
                                        <TextBlock Text="Video&#10;Record"
                   Foreground="White"
                   TextAlignment="Center"
                   FontSize="25"
                   Visibility="{Binding IsVideoRecording,
                       Converter={StaticResource BoolToVisibilityConverter},
                       ConverterParameter=Inverse}"/>
                                        <!-- 녹화 중일 때 -->
                                        <TextBlock Text="⏹ Stop" Foreground="White" TextAlignment="Center" FontSize="25"
                                            Visibility="{Binding IsVideoRecording,  Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        <!-- 경과 시간 (녹화 중일 때만) -->
                                        <TextBlock Text="{Binding VideoRecordingTime}" Foreground="#FF4444" TextAlignment="Center" FontSize="16"
                                            FontWeight="Bold" Visibility="{Binding IsVideoRecording, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Button>
                                <Button Grid.Row="4" Command="{Binding DicomRecordCommand}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource DarkBtn}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDicomRecording}" Value="True">
                                                    <Setter Property="Background" Value="#DC2626"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>

                                    <StackPanel HorizontalAlignment="Center">

                                        <!-- 기본 상태 (녹화 중 아닐 때) -->
                                        <TextBlock Text="Dicom&#10;Record" Foreground="White" TextAlignment="Center" FontSize="25" Visibility="{Binding IsDicomRecording,
                       Converter={StaticResource BoolToVisibilityConverter},
                       ConverterParameter=Inverse}"/>

                                        <!-- 녹화 중일 때 -->
                                        <TextBlock Text="⏹ Stop" Foreground="White" TextAlignment="Center" FontSize="25" Visibility="{Binding IsDicomRecording,
                       Converter={StaticResource BoolToVisibilityConverter}}"/>

                                        <!-- 경과 시간 (녹화 중일 때만) -->
                                        <TextBlock Text="{Binding RecordingTime}" Foreground="#FF4444" TextAlignment="Center" FontSize="16"
                                                FontWeight="Bold" Visibility="{Binding IsDicomRecording,
                       Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Grid>

                        <!-- Setting Panel 오버레이 -->
                        <Grid x:Name="SettingPanel"
                            Visibility="Collapsed" Panel.ZIndex="99"
                            Background="{StaticResource Panel}"
                            VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                            <Grid Margin="0,6,0,6">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="8"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Button Grid.Row="0" HorizontalAlignment="Right"
                                    Width="56" Height="56" Style="{StaticResource IconBtn}"
                                    Command="{Binding ResetSettingCommand}">
                                    <TextBlock Text="⟲" Foreground="White" FontSize="24"/>
                                </Button>

                                <!-- EXPOSURE TIME -->
                                <Border Grid.Row="2" Background="{StaticResource Card}" CornerRadius="12" Padding="8,6">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="EXPOSURE TIME"
                           Foreground="White" FontSize="13" FontWeight="Bold" Margin="0,0,0,4"/>
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="54"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="54"/>
                                            </Grid.ColumnDefinitions>
                                            <Button Grid.Column="0" Content="-" Style="{StaticResource PmBtn}"
                            VerticalAlignment="Stretch"
                            Command="{Binding ExposureDecCommand}"/>
                                            <Border Grid.Column="2" Background="{StaticResource ValueBg}" CornerRadius="12">
                                                <TextBlock Text="{Binding ExposureText}"
                                   Foreground="White" FontSize="22" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <Button Grid.Column="4" Content="+" Style="{StaticResource PmBtn}"
                            VerticalAlignment="Stretch"
                            Command="{Binding ExposureIncCommand}"/>
                                        </Grid>

                                    </Grid>
                                </Border>

                                <!-- GAIN -->
                                <Border Grid.Row="4" Background="{StaticResource Card}" CornerRadius="12" Padding="8,6">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="GAIN"
                           Foreground="White" FontSize="13" FontWeight="Bold" Margin="0,0,0,4"/>
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="54"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="54"/>
                                            </Grid.ColumnDefinitions>
                                            <Button Grid.Column="0" Content="-" Style="{StaticResource PmBtn}"
                            VerticalAlignment="Stretch"
                            Command="{Binding GainDecCommand}"/>
                                            <Border Grid.Column="2" Background="{StaticResource ValueBg}" CornerRadius="12">
                                                <TextBlock Text="{Binding GainText}"
                                   Foreground="White" FontSize="22" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <Button Grid.Column="4" Content="+" Style="{StaticResource PmBtn}"
                            VerticalAlignment="Stretch"
                            Command="{Binding GainIncCommand}"/>
                                        </Grid>

                                    </Grid>
                                </Border>

                                <!-- GAMMA -->
                                <Border Grid.Row="6" Background="{StaticResource Card}" CornerRadius="12" Padding="8,6">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="GAMMA"
                           Foreground="White" FontSize="13" FontWeight="Bold" Margin="0,0,0,4"/>
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="54"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="54"/>
                                            </Grid.ColumnDefinitions>
                                            <Button Grid.Column="0" Content="-" Style="{StaticResource PmBtn}"
                            VerticalAlignment="Stretch"
                            Command="{Binding GammaDecCommand}"/>
                                            <Border Grid.Column="2" Background="{StaticResource ValueBg}" CornerRadius="12">
                                                <TextBlock Text="{Binding GammaValue}"
                                   Foreground="White" FontSize="22" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <Button Grid.Column="4" Content="+" Style="{StaticResource PmBtn}"
                            VerticalAlignment="Stretch"
                            Command="{Binding GammaIncCommand}"/>
                                        </Grid>

                                    </Grid>
                                </Border>

                                <!-- IRIS -->
                                <Border Grid.Row="8" Background="{StaticResource Card}" CornerRadius="12" Padding="8,6">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Text="IRIS"
                           Foreground="White" FontSize="13" FontWeight="Bold" Margin="0,0,0,4"/>
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="54"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="54"/>
                                            </Grid.ColumnDefinitions>
                                            <Button Grid.Column="0" Content="-" Style="{StaticResource PmBtn}"
                            VerticalAlignment="Stretch"
                            Command="{Binding IrisDecCommand}"/>
                                            <Border Grid.Column="2" Background="{StaticResource ValueBg}" CornerRadius="12">
                                                <TextBlock Text="{Binding IrisValue}"
                                   Foreground="White" FontSize="22" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <Button Grid.Column="4" Content="+" Style="{StaticResource PmBtn}"
                            VerticalAlignment="Stretch"
                            Command="{Binding IrisIncCommand}"/>
                                        </Grid>

                                    </Grid>
                                </Border>

                            </Grid>
                        </Grid>
                    </Grid>
                    <!-- end ThumbArea -->

                </Grid>
            </Border>

            <!-- ════════════════════════════════
                 하단 바
            ════════════════════════════════ -->
            <Grid Grid.Row="1" Background="{StaticResource Panel}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="220"/>
                </Grid.ColumnDefinitions>

                <!-- 왼쪽: 네비게이션 토글 + 설정 -->
                <StackPanel Grid.Column="0" Orientation="Horizontal"
                            VerticalAlignment="Center" Margin="12,0,0,0">
                    <Button x:Name="ToggleBtn" Style="{StaticResource IconBtn}"
                            Margin="0,0,10,0" Click="ToggleNav_Click">
                        <TextBlock Text="❮" Foreground="White" FontSize="40" FontWeight="Bold"/>
                    </Button>
                    <Button x:Name="SettingToggleBtn" Style="{StaticResource IconBtn}"
                            Click="ToggleSetting_Click">
                        <TextBlock Text="🔧" FontSize="32" Foreground="White"/>
                    </Button>
                </StackPanel>

                <!-- 오른쪽: LOGOUT + EXIT -->
                <Grid Grid.Column="1" Margin="0,0,8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Style="{StaticResource ExitBtn}"
                            Command="{Binding LogoutCommand}"
                            HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                            Margin="0,0,4,0">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="⏻"      FontSize="20" HorizontalAlignment="Center"/>
                            <TextBlock Text="LOGOUT" FontSize="11" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button Grid.Column="1" Content="EXIT"
                            Style="{StaticResource ExitBtn}"
                            Command="{Binding ExitCommand}"
                            HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                            Margin="4,0,0,0"/>
                </Grid>
            </Grid>

        </Grid>
    </Grid>
</UserControl>

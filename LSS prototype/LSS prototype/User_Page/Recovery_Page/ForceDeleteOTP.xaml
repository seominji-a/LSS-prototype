<Window x:Class="LSS_prototype.User_Page.ForceDeleteOTP"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LSS_prototype.User_Page"
        mc:Ignorable="d"
        Title="OTP"
        Width="960"
        Height="720"
        ResizeMode="NoResize"
        Topmost="True"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>

        <!-- ── Login.xaml 과 완전 동일한 색상 팔레트 ── -->
        <SolidColorBrush x:Key="Bg"             Color="#F3F5F7"/>
        <SolidColorBrush x:Key="Primary"        Color="#173753"/>
        <SolidColorBrush x:Key="PrimaryHover"   Color="#1F4767"/>
        <SolidColorBrush x:Key="PrimaryDown"    Color="#102A3F"/>
        <SolidColorBrush x:Key="Secondary"      Color="#5F6E82"/>
        <SolidColorBrush x:Key="SecondaryHover" Color="#516175"/>
        <SolidColorBrush x:Key="SecondaryDown"  Color="#465669"/>
        <SolidColorBrush x:Key="TextDark"       Color="#1E293B"/>
        <SolidColorBrush x:Key="TextMuted"      Color="#64748B"/>
        <SolidColorBrush x:Key="InputBg"        Color="#FFFFFF"/>
        <SolidColorBrush x:Key="InputBorder"    Color="#D6DEE8"/>

        <!-- ── 입력 박스 Border (Login.xaml 동일) ── -->
        <Style x:Key="InputBoxBorderStyle" TargetType="Border">
            <Setter Property="Background"          Value="{StaticResource InputBg}"/>
            <Setter Property="BorderBrush"         Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness"     Value="2"/>
            <Setter Property="CornerRadius"        Value="12"/>
            <Setter Property="Height"              Value="64"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <!-- ── Primary 버튼 확인 (Login.xaml 동일) ── -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource Primary}"/>
            <Setter Property="Foreground"      Value="White"/>
            <Setter Property="FontSize"        Value="24"/>
            <Setter Property="FontFamily"      Value="Arial"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Height"          Value="64"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12" Padding="18,10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDown}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ── Secondary 버튼 취소 (Login.xaml 동일) ── -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button"
               BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="{StaticResource Secondary}"/>
            <Setter Property="FontSize"   Value="22"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryHover}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource SecondaryDown}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Border Background="{StaticResource Bg}"
            CornerRadius="16"
            SnapsToDevicePixels="True">

        <Grid>

            <!-- ════ 좌측 하단: 회사 정보 (Login.xaml 완전 동일) ════ -->
            <StackPanel HorizontalAlignment="Left"
                        VerticalAlignment="Bottom"
                        Margin="24,0,0,20">
                <StackPanel.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontSize"   Value="12"/>
                        <Setter Property="FontFamily" Value="Arial"/>
                        <Setter Property="Foreground" Value="#94A3B8"/>
                    </Style>
                </StackPanel.Resources>
            </StackPanel>

            <!-- ════ 가운데 콘텐츠 (Login.xaml 동일 Width/구조) ════ -->
            <Grid HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Width="720">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Row 0: 타이틀 -->
                    <RowDefinition Height="32"/>
                    <!-- Row 1: 간격 -->
                    <RowDefinition Height="Auto"/>
                    <!-- Row 2: 안내 박스 -->
                    <RowDefinition Height="22"/>
                    <!-- Row 3: 간격 -->
                    <RowDefinition Height="Auto"/>
                    <!-- Row 4: OTP 입력 -->
                    <RowDefinition Height="22"/>
                    <!-- Row 5: 간격 -->
                    <RowDefinition Height="Auto"/>
                    <!-- Row 6: 버튼 -->
                </Grid.RowDefinitions>

                <!-- Row 0: 타이틀 -->
                <TextBlock Grid.Row="0"
                           Text="OTP"
                           FontSize="72" FontWeight="Bold" FontFamily="Arial"
                           Foreground="{StaticResource Primary}"
                           HorizontalAlignment="Center"/>

                <!-- Row 2: 안내 박스 -->
                <Border Grid.Row="2"
                        Background="{StaticResource InputBg}"
                        BorderBrush="{StaticResource InputBorder}"
                        BorderThickness="2"
                        CornerRadius="12"
                        Padding="20,16">
                    <TextBlock FontSize="18" FontFamily="Arial"
                               Foreground="{StaticResource TextMuted}"
                               LineHeight="30"
                               TextWrapping="Wrap">
                        <Run>강제 삭제는 OTP 가 필요합니다.</Run>
                        <LineBreak/>
                        <Run>고객센터 </Run>
                        <Run FontWeight="Bold" Foreground="{StaticResource Primary}">051-254-1379</Run>
                        <Run> 에 문의하여 OTP 를 발급받으세요.</Run>
                    </TextBlock>
                </Border>

                <!-- Row 4: OTP 입력 -->
                <StackPanel Grid.Row="4">
                    <TextBlock Text="OTP 번호"
                               FontSize="18" FontFamily="Arial"
                               Foreground="{StaticResource TextMuted}"
                               FontWeight="SemiBold"
                               Margin="2,0,0,10"/>
                    <Border Style="{StaticResource InputBoxBorderStyle}">
                        <!-- 숫자 입력 제한은 xaml.cs 에서 처리 -->
                        <TextBox x:Name="TxtOtp"
                                 Text="{Binding OtpInput, UpdateSourceTrigger=PropertyChanged}"
                                 FontSize="28" FontFamily="Arial"
                                 Background="Transparent" BorderThickness="0"
                                 VerticalContentAlignment="Center"
                                 Padding="18,0"
                                 MaxLength="6"
                                 Foreground="{StaticResource TextDark}"
                                 PreviewTextInput="TxtOtp_PreviewTextInput"
                                 DataObject.Pasting="TxtOtp_Pasting"/>
                    </Border>
                </StackPanel>

                <!-- Row 6: 확인(2fr) / 취소(1fr) -->
                <Grid Grid.Row="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Command="{Binding ConfirmCommand}">
                        <TextBlock Text="확인"/>
                    </Button>
                    <Button Grid.Column="2"
                            Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding CancelCommand}">
                        <TextBlock Text="취소"/>
                    </Button>
                </Grid>

            </Grid>
        </Grid>
    </Border>
</Window>

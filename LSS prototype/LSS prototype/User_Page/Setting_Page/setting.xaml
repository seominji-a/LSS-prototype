<Window x:Class="LSS_prototype.User_Page.setting"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LSS_prototype.User_Page"
        mc:Ignorable="d"
        Title="SETTING" Height="748" Width="946"
        WindowState="Maximized"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FontFamily="Arial">

    <Window.Resources>
        <!-- ✅ Login 톤 색상 -->
        <SolidColorBrush x:Key="Bg"           Color="#F3F5F7"/>
        <SolidColorBrush x:Key="Primary"      Color="#173753"/>
        <SolidColorBrush x:Key="PrimaryHover" Color="#1F4767"/>
        <SolidColorBrush x:Key="PrimaryDown"  Color="#102A3F"/>
        <SolidColorBrush x:Key="Secondary"    Color="#5F6E82"/>
        <SolidColorBrush x:Key="TextDark"     Color="#1E293B"/>
        <SolidColorBrush x:Key="TextMuted"    Color="#64748B"/>
        <SolidColorBrush x:Key="InputBg"      Color="#FFFFFF"/>
        <SolidColorBrush x:Key="InputBorder"  Color="#D6DEE8"/>
        <SolidColorBrush x:Key="CardBg"       Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Divider"      Color="#D6DEE8"/>
        <SolidColorBrush x:Key="Accent"       Color="#173753"/>

        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Background"               Value="{StaticResource InputBg}"/>
            <Setter Property="Foreground"               Value="{StaticResource TextDark}"/>
            <Setter Property="CaretBrush"               Value="{StaticResource TextDark}"/>
            <Setter Property="BorderBrush"              Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness"          Value="2"/>
            <Setter Property="FontSize"                 Value="15"/>
            <Setter Property="Height"                   Value="42"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10" Padding="12,0">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IpInputBox" TargetType="TextBox" BasedOn="{StaticResource InputBox}">
            <Setter Property="FontFamily"                 Value="Consolas"/>
            <Setter Property="FontSize"                   Value="16"/>
            <Setter Property="MaxLength"                  Value="23"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="PortInputBox" TargetType="TextBox" BasedOn="{StaticResource InputBox}">
            <Setter Property="FontSize"  Value="15"/>
            <Setter Property="MaxLength" Value="5"/>
        </Style>

        <Style x:Key="MultiInputBox" TargetType="TextBox" BasedOn="{StaticResource InputBox}">
            <Setter Property="Height"                   Value="42"/>
            <Setter Property="AcceptsReturn"            Value="False"/>
            <Setter Property="TextWrapping"             Value="NoWrap"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10" Padding="12,0">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontSize"   Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin"     Value="2,0,0,5"/>
        </Style>

        <Style x:Key="BaseBtn" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height"     Value="52"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ApplyBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="{StaticResource Secondary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height"     Value="52"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#516175"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ResetBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height"     Value="52"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D6DEE8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SaveBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="{StaticResource Primary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height"     Value="42"/>
            <Setter Property="Width"      Value="72"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ExitBtn" TargetType="Button" BasedOn="{StaticResource BaseBtn}">
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="FontSize"   Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width"      Value="100"/>
            <Setter Property="Height"     Value="42"/>
        </Style>

        <!-- ══ MWL Filter 콤보박스 스타일 (App.xaml InputComboBoxStyle 동일) ══ -->
        <Style x:Key="InputComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="FontSize"                Value="15"/>
            <Setter Property="Height"                  Value="42"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding"                 Value="12,0"/>
            <Style.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#EEF3F8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InputComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background"                                  Value="{StaticResource InputBg}"/>
            <Setter Property="BorderBrush"                                 Value="{StaticResource InputBorder}"/>
            <Setter Property="BorderThickness"                             Value="2"/>
            <Setter Property="FontSize"                                    Value="15"/>
            <Setter Property="Foreground"                                  Value="{StaticResource TextDark}"/>
            <Setter Property="Height"                                      Value="42"/>
            <Setter Property="Padding"                                     Value="12,0"/>
            <Setter Property="HorizontalContentAlignment"                  Value="Left"/>
            <Setter Property="VerticalContentAlignment"                    Value="Center"/>
            <Setter Property="Cursor"                                      Value="Hand"/>
            <Setter Property="MaxDropDownHeight"                           Value="260"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility"   Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <Grid SnapsToDevicePixels="True">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                </Grid.ColumnDefinitions>

                                <!-- 전체 터치 영역 -->
                                <ToggleButton Grid.ColumnSpan="2"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Focusable="False"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>

                                <!-- 선택값 -->
                                <ContentPresenter Grid.Column="0"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  IsHitTestVisible="False"/>

                                <!-- 화살표 -->
                                <Border Grid.Column="1"
                                        Background="#F2F4F7"
                                        CornerRadius="0,8,8,0"
                                        IsHitTestVisible="False">
                                    <Path Data="M 0 0 L 6 6 L 12 0 Z"
                                          Fill="#1A3B5D"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                                </Border>

                                <!-- Popup -->
                                <Popup x:Name="Popup"
                                       Placement="Bottom"
                                       VerticalOffset="5"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Fade">
                                    <Border Background="White"
                                            BorderBrush="#DDE2E8"
                                            BorderThickness="1.5"
                                            CornerRadius="10"
                                            MinWidth="{TemplateBinding ActualWidth}">
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <!-- 반투명 오버레이 -->
        <Grid Background="#7F000000"/>

        <!-- 중앙 팝업 -->
        <Border Background="{StaticResource Bg}"
                CornerRadius="18"
                Width="946"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect BlurRadius="32" ShadowDepth="6" Opacity="0.2" Color="#173753"/>
            </Border.Effect>

            <Grid Margin="32,24,32,24">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="24"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="24"/>
                    <RowDefinition Height="1"/>
                    <RowDefinition Height="24"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="24"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ── 타이틀 ── -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Setting" Foreground="{StaticResource Primary}" FontSize="36" FontWeight="Bold"/>
                        <TextBlock Text="DICOM 네트워크 설정" Foreground="{StaticResource TextMuted}" FontSize="12" Margin="2,2,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="1" Width="36" Height="36"
                            Style="{StaticResource BaseBtn}"
                            Background="#E2E8F0"
                            VerticalAlignment="Top"
                            Click="Exit_Click">
                        <TextBlock Text="✕" Foreground="{StaticResource TextMuted}" FontSize="16" FontWeight="Bold"/>
                    </Button>
                </Grid>

                <!-- ── Hospital Name + MWL Filter ── -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="240"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="32"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="240"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="Hospital Name"
                               Foreground="{StaticResource TextDark}"
                               FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="2" Style="{StaticResource InputBox}"
                             Text="{Binding HospitalName, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Grid.Column="4" Content="Save"
                            Style="{StaticResource SaveBtn}"
                            Command="{Binding SaveHospitalCommand}"/>

                    <!-- ★ MWL Filter: TextBox+Save 버튼 → ComboBox 로 교체 -->
                    <!-- TEST SEND 클릭 시 MWL 서버에서 Description 목록을 자동으로 채워줌 -->
                    <!-- 사용자는 콤보박스에서 선택만 가능 (수동 입력 불가) -->
                    <TextBlock Grid.Column="6" Text="MWL Filter"
                               Foreground="{StaticResource TextDark}"
                               FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <ComboBox Grid.Column="8"
                              Style="{StaticResource InputComboBoxStyle}"
                              ItemContainerStyle="{StaticResource InputComboBoxItemStyle}"
                              ItemsSource="{Binding MwlDescriptionItems}"
                              SelectedItem="{Binding SelectedMwlDescription}"/>
                </Grid>

                <!-- ── 구분선 ── -->
                <Border Grid.Row="4" Background="{StaticResource Divider}"/>

                <!-- ── C-STORE / MWL ── -->
                <Grid Grid.Row="6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="1"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- ════ C-STORE ════ -->
                    <Grid Grid.Column="0" Margin="0,0,18,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="4" Height="22" Background="{StaticResource Primary}" CornerRadius="2" Margin="0,0,8,0"/>
                            <TextBlock Text="C-STORE" Foreground="{StaticResource Primary}" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                        <Border Grid.Row="2" Background="{StaticResource CardBg}"
                                CornerRadius="14" Padding="18,18"
                                BorderBrush="{StaticResource InputBorder}" BorderThickness="1.5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="24"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="24"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="24"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="14"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="AET" Style="{StaticResource FieldLabel}"/>
                                <TextBox   Grid.Row="1" Style="{StaticResource InputBox}"
                                           Text="{Binding CStoreAET, UpdateSourceTrigger=PropertyChanged}"/>

                                <Grid Grid.Row="3" Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="IP"   Style="{StaticResource FieldLabel}" Margin="2,0,0,0"/>
                                    <TextBlock Grid.Column="2" Text="PORT" Style="{StaticResource FieldLabel}" Margin="2,0,0,0"/>
                                </Grid>

                                <Grid Grid.Row="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0"
                                             x:Name="CStoreIPTextBox"
                                             Style="{StaticResource IpInputBox}"
                                             Text="     .     .     .     "
                                             GotFocus="IPTextBox_GotFocus"
                                             PreviewTextInput="IPTextBox_PreviewTextInput"
                                             PreviewKeyDown="IPTextBox_PreviewKeyDown"
                                             SelectionChanged="IPTextBox_SelectionChanged"/>
                                    <TextBox Grid.Column="2"
                                             Style="{StaticResource PortInputBox}"
                                             Text="{Binding CStorePort, UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>

                                <TextBlock Grid.Row="6" Text="MyAET" Style="{StaticResource FieldLabel}"/>
                                <TextBox   Grid.Row="7" Style="{StaticResource MultiInputBox}"
                                           Text="{Binding CStoreMyAET, UpdateSourceTrigger=PropertyChanged}"
                                           Tag="RMICG"/>

                                <Grid Grid.Row="9">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Content="TEST SEND" Style="{StaticResource AccentBtn}" Command="{Binding CStoreTestCommand}"/>
                                    <Button Grid.Column="2" Content="SAVE"      Style="{StaticResource ApplyBtn}"  Command="{Binding CStoreApplyCommand}"/>
                                </Grid>
                                <Button Grid.Row="11" Content="RESET" Style="{StaticResource ResetBtn}" Command="{Binding CStoreResetCommand}"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- 세로 구분선 -->
                    <Border Grid.Column="1" Background="{StaticResource Divider}"/>

                    <!-- ════ MWL ════ -->
                    <Grid Grid.Column="2" Margin="18,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="10"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <Border Width="4" Height="22" Background="{StaticResource Primary}" CornerRadius="2" Margin="0,0,8,0"/>
                            <TextBlock Text="MWL" Foreground="{StaticResource Primary}" FontSize="18" FontWeight="Bold"/>
                        </StackPanel>
                        <Border Grid.Row="2" Background="{StaticResource CardBg}"
                                CornerRadius="14" Padding="18,18"
                                BorderBrush="{StaticResource InputBorder}" BorderThickness="1.5">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="24"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="24"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="24"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="14"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="AET" Style="{StaticResource FieldLabel}"/>
                                <TextBox   Grid.Row="1" Style="{StaticResource InputBox}"
                                           Text="{Binding MwlAET, UpdateSourceTrigger=PropertyChanged}"/>

                                <Grid Grid.Row="3" Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="IP"   Style="{StaticResource FieldLabel}" Margin="2,0,0,0"/>
                                    <TextBlock Grid.Column="2" Text="PORT" Style="{StaticResource FieldLabel}" Margin="2,0,0,0"/>
                                </Grid>

                                <Grid Grid.Row="4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="100"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0"
                                             x:Name="MwlIPTextBox"
                                             Style="{StaticResource IpInputBox}"
                                             Text="     .     .     .     "
                                             GotFocus="IPTextBox_GotFocus"
                                             PreviewTextInput="IPTextBox_PreviewTextInput"
                                             PreviewKeyDown="IPTextBox_PreviewKeyDown"
                                             SelectionChanged="IPTextBox_SelectionChanged"/>
                                    <TextBox Grid.Column="2"
                                             Style="{StaticResource PortInputBox}"
                                             Text="{Binding MwlPort, UpdateSourceTrigger=PropertyChanged}"/>
                                </Grid>

                                <TextBlock Grid.Row="6" Text="MyAET" Style="{StaticResource FieldLabel}"/>
                                <TextBox   Grid.Row="7" Style="{StaticResource MultiInputBox}"
                                           Text="{Binding MwlMyAET, UpdateSourceTrigger=PropertyChanged}"
                                           Tag="RMICG"/>

                                <Grid Grid.Row="9">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="8"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Content="TEST SEND" Style="{StaticResource AccentBtn}" Command="{Binding MwlTestCommand}"/>
                                    <Button Grid.Column="2" Content="SAVE"      Style="{StaticResource ApplyBtn}"  Command="{Binding MwlApplyCommand}"/>
                                </Grid>
                                <Button Grid.Row="11" Content="RESET" Style="{StaticResource ResetBtn}" Command="{Binding MwlResetCommand}"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
